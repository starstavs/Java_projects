package homework150.cinema;

import java.time.LocalTime;

public class StandardTickets{

    /*protected LocalTime getMovies(int numberMovie) {

        for (int j = 2; j < movies[numberMovie].length; j++) {
            System.out.println((j - 1) + " - " + movies[numberMovie][j]);
        }
        System.out.println("Select time");
        int option = Integer.parseInt(scan.nextLine());
        if (option <= movies[numberMovie].length) return (LocalTime) movies[numberMovie][option + 1];
        else return null;


    }*/

}
